# 2026-05-29 学习笔记

## 今日主题：Week 4 启动 — 选定 Wallet / Permission 方向，项目设计初稿

**学习目标**：阅读 Wallet / Permission Track Handbook，确认项目方向，输出项目设计文档初稿。

---

## 今日任务清单

- [x] 阅读 Handbook：[Wallet / Permission Track](https://aiweb3.school/zh/handbook/tracks/wallet-permission/)
- [x] 确认项目方向：带权限约束的 Agent 钱包
- [x] 输出项目设计文档初稿（问题定义、目标用户、技术栈、MVP）
- [x] 列出需要补充学习的技术点

---

## Handbook 阅读笔记

### 核心理念

**Agent 不应该拥有"钱包"，它只应该拥有一组可限制、可审计、可撤销的能力。**

钱包、智能账户或权限模块来持有资产和执行权，Agent 只做三件事：提出意图 → 生成候选交易 → 请求有限权限并在规则允许时触发动作。

### 七个核心知识点

#### 1. AI Wallet UX

让用户知道 Agent 在做什么：

- 正在执行的任务是什么
- 会读取、生成、调用什么
- 涉及哪些权限（资产、合约、函数、时间范围）
- 确认后资产和状态的变化
- 存在哪些风险
- 如何撤销和停止

核心原则：**用户不需要理解技术细节，但必须看到权限的边界和后果。**

#### 2. Permission Policy

写给系统执行层看的规则，必须能被程序检查（不是自然语言）：

| 维度 | 说明 |
|---|---|
| 资产范围 | 哪些 token 可以被操作 |
| 金额上限 | 单笔 + 累计 |
| 目标合约 | 白名单合约地址 |
| 函数范围 | 每个合约允许调哪些方法 |
| 价格和滑点 | 交易价格约束 |
| 时间窗口 | 有效期 |
| 频率限制 | 单位时间内操作次数上限 |

#### 3. Session Key

给特定任务临时使用的受限 key，完整流程：

```
主钱包创建 Session Key
  → 设置权限范围（Policy）
    → Agent 使用 Session Key 发起符合规则的操作
      → 钱包 / Smart Account 校验 Policy
        → 权限到期 / 额度用完 / 用户手动撤销 → Session Key 失效
```

#### 4. Safe Guard

执行前判断交易是否符合规则的守卫层：

- **确定性规则**：拦住不该发生的动作（硬约束，无例外）
- **人工确认**：处理规则覆盖不了的灰区（软约束，需要人介入）

设计原则：能在代码里拦住的就不要问人，代码拦不住的才升级到人工。

#### 5. ERC-4337 Workflow

简化流程：

```
Agent 生成意图或调用候选
  → 前端/后端包装生成 UserOperation
    → Smart Account 校验签名、nonce、权限和执行逻辑
      → Bundler 把 UserOperation 打包提交给 EntryPoint
        → Paymaster 按规则代付 gas
          → 链上执行，记录结果和失败原因
```

#### 6. Pre-transaction Simulation

签名前必须模拟交易：

- 模拟结果要翻译成人能理解的语言
- **关键字段必须来自结构化解析和链上模拟**，不能只靠自然语言总结
- 防止模型对模拟结果做自由发挥或掩盖风险

#### 7. Recovery / Revocation

在给 Agent 权限的同时，必须设计恢复和撤销机制：

- 用户可随时手动撤销 Session Key
- Session Key 过期自动失效
- 额度用完自动失效
- 异常检测触发自动冻结

### 跟 Week 3 内容的衔接

Handbook 的七个知识点跟 Week 3 形成精确对应：

| Handbook 知识点 | Week 3 对应内容 |
|---|---|
| AI Wallet UX | Day 11 Web3 Tool Use（工具输出 → 用户可见） |
| Permission Policy | Day 10 AI Security（权限模型用代码定义，不靠 Prompt） |
| Session Key | Day 9 AA Smart Account + Day 10 Agent Wallet |
| Safe Guard | Day 10 AI Security（防御纵深） |
| ERC-4337 | Day 9 AA UserOperation 实操 |
| Pre-transaction Simulation | Day 10 Agent Wallet（执行前验证） |
| Recovery / Revocation | Day 10 AI Security（兜底机制） |

Week 3 搭的是概念骨架，Week 4 Handbook 给的是工程落地细节。

---

## 项目方向确认

**选定 Track**：Wallet / Permission

**核心命题**：如何给 AI Agent 一个"受限但够用"的链上钱包——能自主操作，但不能越界。

**为什么选这个方向**：
- Week 3 的 Agent Wallet + AI Security 已经打下了概念基础
- Day 9 AA 实操可以直接复用
- 这个方向可以做最小可验证原型，不依赖复杂外部系统

---

## 遗留问题

1.

---

## 打卡草稿

> 复制以下部分到 WCB Learning 页面提交：

**今日学习**：
阅读 Wallet / Permission Track Handbook，确认 Hackathon 项目方向为 Wallet / Permission。设计了一个带权限约束的 Agent 钱包原型——Agent 只能在限额、白名单、时间窗口内自主操作。

**核心收获**：
读完 Wallet / Permission Track Handbook，最重要的认知转变：Agent 不应该"拥有"钱包，它只应该拥有一组可限制、可审计、可撤销的能力。钱包、智能账户或权限模块来持有资产和执行权，Agent 只提出意图、生成候选交易、请求有限权限，并在规则允许时触发动作。

七个核心知识点形成完整闭环：AI Wallet UX（用户看到 Agent 在做什么）→ Permission Policy（七维约束用代码写死）→ Session Key（临时受限 key）→ Safe Guard（确定性规则+人工灰区）→ ERC-4337 Workflow（UserOp 打包执行）→ Pre-transaction Simulation（结构化模拟，不靠模型总结）→ Recovery/Revocation（从设计阶段就内置撤销）。

对应项目设计文档已输出，方向是 Safe Agent Wallet——用 Smart Account + Session Key + Safe Guard 实现三层权限架构。

---
