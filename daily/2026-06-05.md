# 2026-06-05 学习笔记

## 今日主题：Day 19 — Safe Agent Wallet 前端联调 & 链上验证

**目标**：配置 .env.local，实际跑通 Safe Agent Wallet 完整前端流程，从交易输入到 Sepolia 链上执行。

---

## 部署过程

### 1. 环境配置

从 `.env.local.example` 创建 `.env.local`，填入三个变量：

| 变量 | 来源 |
|------|------|
| `PIMLICO_API_KEY` | Week 4 CLI 版已有，复用 |
| `OWNER_PRIVATE_KEY` | EOA 主钱包私钥（CLI 版 `PRIVATE_KEY` 改名） |
| `PRIVATE_KEY` | Web 流程用不到（每次自动生成 Session Key） |

注意：前端变量名 `OWNER_PRIVATE_KEY` 和 CLI 版 `PRIVATE_KEY` 是同一把 EOA 私钥——重构时改的命名，开始时填错了值导致 500。

### 2. 修复的三个问题

#### Bug 1：Simulation 类型映射错误（关键修复）

**现象**：Simulation 显示"失败 ✗"但没有错误信息，实际上 `eth_call` 是成功的。

**原因**：`runPreTxSimulation` 返回的 `SimulationReport` 对象没有顶层 `success` 字段（`success` 嵌套在 `callSim.success` 里）。前端 `Simulation` 是 discriminated union（`SimulationSuccess | SimulationFailed`），检查 `simulation.success` 拿到 `undefined`（falsy），直接走失败分支。

**修复**：API 层（route.ts）在序列化前把 `SimulationReport` 映射到前端类型：

```typescript
const simulationResponse = simulation.callSim.success
  ? { success: true, callSim, gasEstimate, summary, riskLevel, warnings }
  : { success: false, error: simulation.callSim.revertReason || "..." };
```

这个 bug 如果没修，即使链上执行成功，前端也始终显示"Simulation 失败"。说明 discriminated union 类型定义和运行时数据的一致性是前端开发中容易漏的坑。

#### Bug 2：Smart Account 余额为 0

`eth_call` 模拟转账 0.001 ETH 时 revert——Smart Account 合约已部署但余额为 0。从 Sepolia 水龙头领了 0.05 ETH 解决。

关键认知：Paymaster 只赞助 gas，转账的 ETH 必须从 Smart Account 余额出。

#### Bug 3：RPC 节点网络问题

Phase 1（eth_call）过，Phase 2（sendUserOperation）失败——`sepolia.rpc.thirdweb.com` 在国内不稳定。换成 `ethereum-sepolia-rpc.publicnode.com` 解决。

### 3. 完整流程验证

```
① 交易输入：to=0xf9AD...9afA, value=0.001 ETH
② Safe Guard：通过 ✓（七维约束全部通过）
③ Simulation：通过 ✓（余额变化 0.05→0.049 ETH，风险 LOW）
④ 用户确认：点击
⑤ 链上执行：已提交 ✓
   txHash: 0x73b15d4ef83c9a222760b4ca17516f581ac1a2227b093368ff86977e83d6c26e
```

Sepolia Etherscan 已确认。

---

## 核心收获

### 1. Discriminated union 必须保证运行时数据匹配

TypeScript 编译时能区分 `SimulationSuccess | SimulationFailed`，但运行时数据是 `JSON.parse` 出来的，没有类型保护。如果后端返回的 JSON 和类型定义不一致，组件逻辑就会静默出错。修复方式应该是 API 层做数据映射，而不是前端防御性地兼容两种形状。

### 2. RPC 节点选择影响可靠性

同一个交易，Phase 1（eth_call）成功、Phase 2（sendUserOperation + waitForReceipt）失败——因为 Phase 2 调用链更长、RPC 次数更多。国内访问海外 RPC 时，选稳定的公共节点（如 publicnode）比单一第三方 RPC 更可靠。

### 3. ERC-4337 费用模型：Paymaster ≠ 免费

Paymaster 赞助的是 UserOperation 的 gas 费（verificationGas + callGas），不赞助转账金额。Smart Account 余额必须 ≥ 转账金额。

---

## 遗留问题

- [ ] Paymaster 未生效（显示"无赞助"），应该是 Pimlico 该 API key 的 plan 不支持
- [ ] Phase 2 执行中前端只显示"执行中..."，无子步骤进度（30-60s 等待无反馈）
- [ ] session 缓存无 TTL（demo 可接受）

---

## 明日计划

- [ ] 录屏/截图，准备 Hackathon 演示材料
- [ ] 写项目总结文档
- [ ] 或继续深入其他 Track
