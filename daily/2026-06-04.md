# 2026-06-04 学习笔记

## 今日主题：Day 18 — Safe Agent Wallet 前端 UI

**目标**：给 CLI 版本的 Safe Agent Wallet MVP 加一个 Web 前端，支持完整的「交易输入 → Guard → Simulation → 确认 → 链上执行」流程。

---

## 设计阶段

### 方案选择

3 个候选方案，最终选 Next.js App Router 单项目：

- **A. Next.js**（选中）：API Routes 直接 import 现有 week4 TS 模块，一套代码跑起来
- B. Express 后端 + Vite React 前端：前后端分离，维护成本高
- C. 纯 HTML 页面：最简单，但演示效果差

### 架构设计

```
frontend/
├── app/
│   ├── api/execute/route.ts    # 唯一 API：POST /api/execute
│   ├── layout.tsx / page.tsx   # 单页面，状态机驱动
│   └── globals.css             # 深色主题
├── components/                  # 4 个展示组件
│   ├── TxForm                  # 交易输入 + 前端校验
│   ├── GuardResult             # Safe Guard 检查结果
│   ├── SimulationReport        # 链上模拟 + Gas 预估 + 风险等级
│   └── TxResult                # 交易结果 + Etherscan 链接
└── lib/types.ts                # API 响应类型（bigint → string）
```

### 两阶段 API 设计

一个接口 `POST /api/execute`，两次调用：

1. `autoConfirm: false` → 后端跑 **Safe Guard + Simulation**，返回报告给用户看
2. 用户确认后 `autoConfirm: true` → 后端**直接执行链上交易**，不重复 guard/simulation

这样做的好处：
- Guard/Simulation 结果以结构化 JSON 返回，前端负责渲染，不靠 Agent 自然语言总结
- 第二阶段不重复计算，避免不必要的 RPC 调用
- 两个阶段之间用 `sessionId` 关联（后端内存 Map，demo 用）

### 安全约束（设计时写入 spec）

- `console.log` 只输出地址（public key），禁止输出私钥值
- API response 不包含 `PRIVATE_KEY` / `OWNER_PRIVATE_KEY` 字段
- 500 错误返回到客户端时用通用消息，不暴露内部细节

---

## 实现阶段

### 技术决策

| 问题 | 方案 |
|------|------|
| week4 模块在 Next.js 项目外部，如何 import？| `next.config.js` webpack 规则扩展，把 `week4/` 目录纳入编译范围 |
| bigint 不能 JSON 序列化 | 递归 `serializeBigInt` 函数，所有 bigint → string |
| 客户端组件不能用 week4 类型 | 新建 `lib/types.ts`，定义纯前端类型（所有 bigint 已序列化为 string） |
| Session 跨请求状态 | 模块级 `Map<string, SessionState>`，标注 "demo only" |

### 13 commits, 17 source files

```
5e302dd feat: scaffold Next.js frontend project
803b937 feat: add shared API response types
d5f1146 feat: add POST /api/execute route
9368a14 feat: add root layout and global styles
80ed3fc feat: add TxForm component with validation
6e964a2 feat: add GuardResult component
717e384 feat: add SimulationReport component
403e510 feat: add TxResult component
301b044 feat: add main page with state machine flow
cbdb60c feat: add .env.local.example template
```

修复轮：
```
4d34d91 fix: defer session delete to after success, sanitize 500 error messages
4447bbf chore: add .next/ to gitignore
4fa5349 fix: discriminated union for Simulation type, handle responses without stage field
```

### 代码 Review 流程

使用了 Subagent-Driven Development：
- 每个 task 派发独立 implementer subagent
- Spec compliance review（检查是否严格按 spec 实现）
- Code quality review（代码质量、安全、架构）
- 发现问题 → implementer 修复 → re-review → 通过

发现的关键问题及修复：
1. Session 提前删除导致失败无法重试 → 移到成功后删除
2. 500 错误暴露内部信息 → 改为通用消息
3. `Simulation` 类型在失败路径上与实际返回值不匹配 → discriminated union

---

## 核心收获

### 1. API 设计先于实现

spec 中逐字段定义了请求和响应的 JSON schema，包括三个 stage 状态、每个字段的类型。写代码时照着 spec 翻译就行，不需要边写边想。

### 2. 类型安全不是事后补的

`Simulation` 最初是一个 flat interface，成功和失败路径共用同一组字段。review 发现失败时 API 根本不返回 `callSim`/`gasEstimate` 等字段——类型说了谎。改成 discriminated union 后，TypeScript 能根据 `success` 值自动窄化类型。

### 3. "能在代码里拦住的就不要问人"

这个原则在 security review 中也适用：日志里不输出私钥、错误信息不暴露内部栈、类型系统不撒谎——都是代码层能拦住的东西，不需要靠人工检查。

### 4. 两阶段 API 模式

不只是 Web3 特有的 pattern。任何需要"先展示结果再让用户确认"的流程都适用：
- Phase 1: 只读操作（guard + simulation），不改变链上状态
- Phase 2: 写操作（execution），只在用户确认后执行

### 5. Dark theme 配色一致性的价值

5 个组件的 CSS Module 都用了同一套 color palette（`#0d1117` 背景、`#161b22` 卡片、`#30363d` 边框、`#c9d1d9` 文字）。没有用 CSS 变量或共享类，但不影响一致性——因为颜色语义是统一的（绿=通过、红=拒绝、黄=警告）。

---

## 遗留问题

- [ ] session 缓存没有 TTL，长时间运行会内存泄漏（demo 可接受，生产需要 Redis + expiry）
- [ ] Phase 2 执行时前端只显示 "执行中..."，没有子步骤进度（30-60s 等待无反馈）
- [ ] 未实际运行验证（需要 .env.local 配置 PIMLICO_API_KEY 等）

---

## 明日计划

- [ ] 配置 .env.local，实际跑一遍完整流程
- [ ] 截图/录屏，准备 Hackathon 演示材料
- [ ] 或继续深入其他 Track
