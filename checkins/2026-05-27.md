# 2026-05-27 学习打卡

## 今日主题

**AI Security + Agent Wallet · Session Key 权限边界**：理解为什么 AI Agent 不能直接持有 EOA 私钥，并设计一个受限、可撤销、可过期的最小授权方案。

---

## 今日完成

✅ 阅读 Handbook：AI Security + Agent Wallet  
✅ 梳理 Agent 上链的核心风险链路：Prompt Injection → 工具调用 → 钱包签名 → 链上不可逆执行  
✅ 设计一个具体的 Session Key 权限边界表（合约 / 方法 / 限额 / 时效 / 撤销）  
✅ 对比 `transfer`、`approve`、任意合约调用 的风险差异  
✅ 完成今日笔记和打卡草稿  

---

## 核心收获

今天真正想明白的是：AI Agent 上链的关键不是“让它会签名”，而是“让它只能在可接受损失范围内签名”。

EOA 私钥的问题不是不能自动化，而是权限太大、不可细分、泄露后几乎等于账户失控；Session Key 的价值在于把权限拆成特定合约、特定方法、单笔/累计额度、有效期和撤销机制。这样即使 Agent 被 Prompt Injection 诱导，真正能落到链上的动作仍然被限制在一个很小的安全盒子里。

我今天设计的最小方案是：在 Sepolia 上给 Agent 一个只允许调用指定 ERC-20 合约 `transfer` 的 Session Key，禁止 `approve`、`transferFrom` 和任意合约调用；单笔 ≤ 5 USDC、24 小时累计 ≤ 20 USDC、仅限白名单地址、2 小时自动过期。这样就把“Agent Wallet 的安全边界”从抽象概念变成了明确规则。

---

## 遗留问题

1. Session Key 撤销哪些场景必须上链，哪些场景链下失效就够？
2. 更复杂的 DeFi 操作需要在限额之外再加哪些约束？
3. 累计额度限制通常由账户合约实现，还是由插件模块实现？

---

## Handbook Feedback

- AI Security 和 Agent Wallet 连着读很顺，因为一个讲“为什么危险”，一个讲“怎么收权”
- 如果 Handbook 能补一个 Session Key 权限边表示例，新手会更容易从概念走到设计
- `approve` 比 `transfer` 更危险这个点，值得讲得更显性一点

---

## 明日计划

- [ ] 回补 Web3 Tool Use，把 RPC / 钱包 / 合约调用能力整理成 Agent 工具链
- [ ] 或继续深挖 Session Key 的撤销机制与额度限制实现方式

---

*提交到 WCB Learning 平台时，可以直接复制「核心收获」部分*
